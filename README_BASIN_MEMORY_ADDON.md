# SRCF v11: Basin Memory Addon

Добавляет файл:

```text
closure_basin_memory_benchmark_v1.py
```

Это не очередной anomaly benchmark и не обычный edge repair. Это проверка идеи:

```text
память = сходимость повреждённой системы отношений к правильному relation-basin
```

Запуск быстрый:

```bash
bash scripts/run_basin_memory_quick.sh
```

Запуск сильнее:

```bash
bash scripts/run_basin_memory_hard.sh
```
